import { ThemeToken } from './ThemeToken.js'

const token = new ThemeToken('awesome', 10, 'sizes')

token.prefix = 'fusion'

token.computedValue
token.prefix
token.scale
token.name
token.value
token.variable
void token.toString()
