// import createCss from '../src/index';

describe('Themes', () => {
	/** @todo fix false positives - there are no tests. */
	describe('Create an additional theme', () => {
		test('Supports creating an additional theme', () => {
			expect(true).toBe(true);
		});
	});
});
